# 仅包含信令服务器的轻量级镜像

FROM python:3.11-slim

WORKDIR /app

# 安装依赖
RUN pip3 install --no-cache-dir flask flask-cors

# 复制信令服务器
COPY signaling_server.py /app/

# 设置可执行权限
RUN chmod +x /app/signaling_server.py

EXPOSE 9355

CMD ["python3", "/app/signaling_server.py", "9355"]

